package testNG;

import org.testng.ITestListener;

//lots of methods inside ITestListener - like what to do after pass,fail etc of the test case
public class ListenersExample implements ITestListener {

}
